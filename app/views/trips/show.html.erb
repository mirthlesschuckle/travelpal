<div class="container" style="width: 500px; margin-top: 200px; margin-bottom: 245px;">
  <h1 style="margin-bottom: 50px; text-align: center;">Your trips</h1>

  <% @trips.where(user_id: current_user.id).each do |trip| %>
    <% destination = Destination.find(trip.destination_id) %>
    <p><strong>Destination:</strong><%= destination.name %></p>
    <p><strong>Description:</strong><%= destination.description %></p>
    <p><strong>Start Date:</strong><%= trip.start_date %></p>
    <p><strong>End date:</strong><%= trip.end_date %></p>
  <% end %>

  <%= link_to "Edit", edit_trip_path(@trip), style: "background-color: #2156af; border-radius: 20px; border: none; color: #FFFFFF; font-size: 20px; font-weight: bold; padding: 10px 20px; text-decoration: none; transition: all 0.3s ease-in-out;" %>

</div>
